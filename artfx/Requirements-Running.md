## Requirement-Running
### Prerequisites
- Python 3.7+ 
- FastAPI-compatible ASGI server (Uvicorn recommended)

### Running the Application
1. Install dependencies (FastAPI, uvicorn)
2. Start with: `uvicorn main:app --reload`
3. Access application at `http://localhost:8000`